__pycache__/
*.pyc
.venv/
venv/
.env
logs/*.jsonl
.pytest_cache/

# Catalyst deploy config — contains secrets, keep local
app-config.json
.catalystrc

# Vendored linux wheels for Catalyst AppSail — regenerate, do not commit (44MB)
vendor/
